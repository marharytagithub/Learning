#Procedural approach#

1. create function. the function has 3 words. first we echo 1st, 2nd, 3rd word on new line. then we show all 3 words together 

2. create function. the function check current time. if time is before 14:00 - echo 'before', if after 14:00 - echo 'after' 

3. do 1st task with array, sort words alphabetically, do as function<br />

4. sort vs asort vs ksort

5. learn array foreach 

6. create function. the function has 3 words, use array, the words are sorted alphabetically and returned as one concatenated line, use return

7. create function. the function has 3 words, use array, the words are sorted alphabetically and returned as 3 lines, one by one, use return

8. learn "for" 

9. learn php data types 

10. create function. in function we iterate numbers from 0 to 12. if number 8 is matched - we return it and stop the function

11. read about function parameters

12. create function which will call another function

13. create function with 1 parameter, function will show numbers from 0 to parameter

14. create function which will have three parameters and return sum of that parameters

15. create function dump(); this function will show in readable format the parameter which we pass to this function

16. create function which will show which type is the parameter 

17. create function which will return current time

18. function variable visibility scope

19. create function. the function compare two arrays and return diff

20. function variable visibility scope

21, create function. the function has one parameter name. the function return html page with this name

22. create function. function has one parameter (int). depending of this parameter we return word one, two, three, four or not found. use switch

23. learn about global variables

24. declare global variables country. create function which has one parameter name and return string $ name live in country

25. php dynamic typization 

26. php error severity levels

27. create folders

28. php type casting

29. create function with 1 parameter. the function return how many times letter 'a' matched in parameter

30. create function. the function capitalized all words in sentence

31. create function. the function replaces word 'one' to 'uuu' in all sentence

32. create function which returns html form with predefined input fields number

33. create function. the function build form with select. in select we have values 1 => 'one', 2 => 'two, three => 'three' where 1, 2, 3 - values, 'one, two, three' - labels

34. create function. the function build form with radio buttons where number of radiobuttons are set in function parameter

35. create function which draws a fee-tree using letter 'O'

36. create function. the function build text with <p>. every <p> is element of array 

37. create function. the function can accept any numbers, but shows only even numbers

38. variable type is defined when we set the value

39. create function. the function has one parameter array. the function replace all keys in this array with words (0 => zero, 1 => one...)

40. create function. use while

41. use while do

42. postincrement, preincrement

43. include, include_once, require, require_once

44. create function which uses function dump, use include

45. create file, which has different variables. include this file and use variables in function

46. read about absolute and relative path

47. obligatory and arbitrary function params

48. learn html <img> and <a href>

49. create function. the function accept parameter text and return array of words

50. exit, break, die, return

51. html5 validation (max lengh)

52. type hinting

53. control Structures 

54. compilation

55. php dynamic typing

56. php type casting

57. html 

58. create function which accept as parameter date in format 2015.04.07 and return back line similar to '7 April'. for day numbers use only range 1 - 10

59. create function which write the parameter to file. the filename is current datetime like 20150407_0234  

60. php server variables

61. php mail function 

62. php error log

63. microtime, timestamp. convert microtime to date

64. refactoring 2015.04.07.1

65. and, or, &&, ||, &, |, nor, xor

66. how select several files

67. for create array with ten elements, elements values are arbitrary

68. continue

69. assembler

70. string+string, string+array...

71. constant which types 

72. http request structure

73. base64

74. create function. the function take as parameter file name and return the file extension

75. php header

76. create function. the function accept as parameter you age and return year when you was born. for example I'm 30 => you was born in 1984 

77. post, get, submit form data, upload file

78. http://on-line-teaching.com/php/global.arrays.htm

79. create function. the function accept parameter text. and write every word in a new file, try catch. turn off file permission

80. create page with form (name, phone). submitted form data is saved to text file. file name is current datetime

81. session

82. cookie

83. create function. the function redirect browser to the link, which is function param 

84. create form which submit result to google.com page result list

85. create form which submit data and save it to session 

86. how to stop for, how to stop function

87. regular expressions

88. php and js

89. create function which validate phone number using regular expression 

90. create function which save form post data to cookies

91. create function which get saved form data from cookies (param default - all, param -> name cookies)

92. error handling, try {} catch 

93. create website specification in google docs (idea, target audience, functionality, technology, hosting)

94. read refactoring 

95. create php function. the function generates js code which alert some message

96. iframe

97. create main website page, adaptive design

98. create Contacts page, with Contact me form, the form data is saved to file 

99. read old topics

100. contacts page

101. adaptive site structure

102. after contact form saved if ok - redirect to Thank you page

103. pagination get parameter  http://r/r/r/r/r/r/r/r/r/r/r/r/r/r/r/ru.html.net/tutorials/php/lesson10.php

104. gallery

105. website refactoring 

106. http request 

107. website refactoring 

